{
  "name": "nix-couchdb-replicate",
  "version": "1.1.0",
  "description": "Does a one-off replication of all databases in one CouchDB installation to another CouchDB installation",
  "main": "index.js",
  "author": "Nikhil Sinha",
  "license": "ISC",
  "dependencies": {
    "cli-progress": "^3.9.0",
    "nano": "^9.0.3"
  },
  "bin": {
    "nix-couchdb-replicate": "./index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nsinha91/nix-couchdb-replicate.git"
  },
  "keywords": [
    "couchdb",
    "replication",
    "cli"
  ],
  "devDependencies": {
    "dotenv": "^16.4.4",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-n": "^16.6.2",
    "eslint-plugin-promise": "^6.1.1",
    "prettier": "^3.2.5"
  },
  "comments": {
    "devDependencies": {
      "dotenv": "Installed in devDependencies rather than dependencies because only the setup script uses the environment, not the app itself.",
      "eslint": "Main eslint package. Installed on initialization of eslint (npm init @eslint/config).",
      "eslint-config-prettier": "Turns off all rules that are unnecessary or might conflict with prettier.",
      "eslint-config-standard": "eslint style guide that enforces Javascript Standard Style (https://standardjs.com/). Installed on initialization of eslint (npm init @eslint/config).",
      "eslint-plugin-import": "Installed on initialization of eslint (npm init @eslint/config).",
      "eslint-plugin-n": "Installed on initialization of eslint (npm init @eslint/config).",
      "eslint-plugin-promise": "Installed on initialization of eslint (npm init @eslint/config).",
      "prettier": "Main prettier package."
    }
  }
}
